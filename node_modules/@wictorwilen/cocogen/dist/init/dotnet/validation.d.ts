import type { PropertyType } from "../../ir.js";
/** Build C# validation literals for string constraints. */
export declare function buildCsStringConstraintsLiteral(prop: {
    minLength?: number;
    maxLength?: number;
    pattern?: {
        regex: string;
        message?: string;
    };
    format?: string;
}): {
    minLength: string;
    maxLength: string;
    pattern: string;
    format: string;
    hasAny: boolean;
};
/** Build C# validation literals for number constraints. */
export declare function buildCsNumberConstraintsLiteral(prop: {
    minValue?: number;
    maxValue?: number;
}): {
    minValue: string;
    maxValue: string;
    hasAny: boolean;
};
/** Wrap a C# parse expression with validation when constraints exist. */
export declare function applyCsValidationExpression(prop: {
    name: string;
    type: PropertyType;
    minLength?: number;
    maxLength?: number;
    pattern?: {
        regex: string;
        message?: string;
    };
    format?: string;
    minValue?: number;
    maxValue?: number;
}, expression: string, sourceLiteral: string): string;
//# sourceMappingURL=validation.d.ts.map