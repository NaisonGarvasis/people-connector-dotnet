import type { ConnectorIr } from "../ir.js";
export type InitOptions = {
    tspPath: string;
    outDir: string;
    projectName?: string;
    force?: boolean;
    usePreviewFeatures?: boolean;
    inputFormat?: "csv" | "json" | "yaml" | "rest" | "custom" | undefined;
};
export type UpdateOptions = {
    outDir: string;
    tspPath?: string;
    usePreviewFeatures?: boolean;
    includeScaffold?: boolean;
};
/** Update an existing TS project from a schema. */
export declare function updateTsProject(options: UpdateOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Update an existing .NET project from a schema. */
export declare function updateDotnetProject(options: UpdateOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Update an existing REST project from a schema. */
export declare function updateRestProject(options: UpdateOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Update an existing project based on its cocogen config. */
export declare function updateProject(options: UpdateOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Initialize a REST-only project from a schema. */
export declare function initRestProject(options: InitOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Initialize a TS project from a schema. */
export declare function initTsProject(options: InitOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
/** Initialize a .NET project from a schema. */
export declare function initDotnetProject(options: InitOptions): Promise<{
    outDir: string;
    ir: ConnectorIr;
}>;
//# sourceMappingURL=init.d.ts.map