import { writeFile } from "node:fs/promises";
import path from "node:path";
import { COCOGEN_CONFIG_FILE, projectConfigContents } from "../project-config.js";
/** Shared generator base for language-specific emitters. */
export class CoreGenerator {
    context;
    constructor(context) {
        this.context = context;
    }
    get outDir() {
        return this.context.outDir;
    }
    get ir() {
        return this.context.ir;
    }
    get settings() {
        return this.context.settings;
    }
    /** Write cocogen.json for the generated project. */
    async writeProjectConfig(tspPath) {
        await writeFile(path.join(this.outDir, COCOGEN_CONFIG_FILE), projectConfigContents(this.outDir, tspPath, this.lang, this.ir.connection.inputFormat), "utf8");
    }
}
//# sourceMappingURL=core.js.map