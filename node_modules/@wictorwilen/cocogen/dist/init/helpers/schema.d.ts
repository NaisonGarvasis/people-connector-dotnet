import type { ConnectorIr, GraphOperation, PropertyType } from "../../ir.js";
/** Build the Graph base URL for the selected API version. */
export declare function graphBaseUrl(ir: ConnectorIr): string;
/** Resolve the Graph API version for a specific operation. */
export declare function graphApiVersionForOperation(ir: ConnectorIr, operation: GraphOperation): string;
/** Build the Graph base URL for a specific operation. */
export declare function graphBaseUrlForOperation(ir: ConnectorIr, operation: GraphOperation): string;
/** Create the external item schema payload for Graph provisioning. */
export declare function schemaPayload(ir: ConnectorIr): unknown;
/** Map property types to Graph schema enum names. */
export declare function toGraphPropertyTypeEnumName(type: PropertyType): string;
/** Map property types to OData collection type strings. */
export declare function toOdataCollectionType(type: PropertyType): string | null;
/** Map property types to C# RowParser function names. */
export declare function toCsParseFunction(type: PropertyType): string;
/** Emit C# value expressions for schema payloads. */
export declare function toCsPropertyValueExpression(type: PropertyType, csPropertyName: string): string;
//# sourceMappingURL=schema.d.ts.map