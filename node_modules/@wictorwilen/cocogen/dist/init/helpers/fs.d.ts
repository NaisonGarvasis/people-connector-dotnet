/** Remove a file if it exists; ignore missing files. */
export declare function removeIfExists(filePath: string): Promise<void>;
/** Ensure output directory exists and is empty unless forced. */
export declare function ensureEmptyDir(outDir: string, force: boolean): Promise<void>;
/** Copy the TypeSpec schema into the output directory. */
export declare function updateSchemaCopy(outDir: string, tspPath: string): Promise<void>;
//# sourceMappingURL=fs.d.ts.map