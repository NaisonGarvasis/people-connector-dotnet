import type { SourceDescriptor } from "../shared-types.js";
/** Emit a TS literal for JSONPath or CSV header sources. */
export declare function buildSourceLiteral(source: SourceDescriptor): string;
/** Emit a C# literal for JSONPath or CSV header sources. */
export declare function buildCsSourceLiteral(source: SourceDescriptor): string;
/** Emit a TS literal for source transforms. */
export declare function buildSourceTransformsLiteral(source: Pick<SourceDescriptor, "transforms">): string | null;
/** Emit a C# literal for source transforms. */
export declare function buildCsSourceTransformsLiteral(source: Pick<SourceDescriptor, "transforms">): string | null;
//# sourceMappingURL=source.d.ts.map