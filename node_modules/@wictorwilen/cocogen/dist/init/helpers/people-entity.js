export const TS_INDENT = "  ";
export const CS_INDENT = "    ";
/** Collect all person-entity fields from a nested object tree. */
export function collectPersonEntityFields(node) {
    const collected = [];
    const visit = (value) => {
        for (const entry of Object.values(value)) {
            if (typeof entry === "object" && entry && "path" in entry) {
                collected.push(entry);
                continue;
            }
            if (typeof entry === "object" && entry && !Array.isArray(entry)) {
                visit(entry);
            }
        }
    };
    visit(node);
    return collected;
}
//# sourceMappingURL=people-entity.js.map