import type { PropertyType } from "../ir.js";
/** Map property types to TypeScript types used in templates. */
export declare function toTsType(type: PropertyType): string;
/** Map property types to C# types used in templates. */
export declare function toCsType(type: PropertyType): string;
/** Convert an arbitrary name into a C#-friendly identifier. */
export declare function toCsIdentifier(name: string): string;
/** Convert a name to PascalCase for C# symbols. */
export declare function toCsPascal(name: string): string;
/** Choose a unique C# property name avoiding collisions. */
export declare function toCsPropertyName(name: string, itemTypeName: string, used: Set<string>): string;
/** Convert an arbitrary name into a TypeScript identifier. */
export declare function toTsIdentifier(name: string): string;
/** Resolve a C# schema folder name from the connection name. */
export declare function toSchemaFolderName(connectionName: string | undefined): string;
/** Resolve a TS schema folder name from the connection name. */
export declare function toTsSchemaFolderName(connectionName: string | undefined): string;
/** Normalize a project name into a C# namespace. */
export declare function toCsNamespace(projectName: string): string;
//# sourceMappingURL=naming.d.ts.map