export function normalizeInputFormat(raw) {
    if (!raw)
        return "csv";
    const value = raw.trim().toLowerCase();
    if (value === "csv" || value === "json" || value === "yaml" || value === "custom" || value === "rest")
        return value;
    throw new Error("Invalid input format. Expected csv, json, yaml, rest, or custom.");
}
//# sourceMappingURL=input-format.js.map